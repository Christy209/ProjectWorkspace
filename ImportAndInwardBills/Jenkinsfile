pipeline {
    agent any

    environment {
        // Set Maven home
        MAVEN_HOME = "C:\\apache-maven-3.9.10\\bin\\mvn"
    }

    stages {

        stage('Init') {
            steps {
                script {
                    echo "🟢 Selected Branch  : main"
                    echo "🟢 Selected TestCase: TC01_LodgeTheBillUsingImportandInwardMenu"
                }
            }
        }

        stage('Checkout') {
            steps {
                checkout([
                    $class: 'GitSCM',
                    branches: [[name: '*/main']],
                    doGenerateSubmoduleConfigurations: false,
                    extensions: [],
                    userRemoteConfigs: [[
                        url: 'https://github.com/Christy209/ProjectWorkspace.git'
                    ]]
                ])
            }
        }

        stage('Use Managed Config') {
            steps {
                configFileProvider([configFile(fileId: 'config.properties', variable: 'CONFIG_FILE')]) {
                    script {
                        echo "📄 Copying Jenkins Managed config to workspace"
                        bat "copy /Y \"%CONFIG_FILE%\" ImportAndInwardBills\\resources\\config.properties"

                        // Optional: read and set env variables
                        def props = readProperties file: 'ImportAndInwardBills/resources/config.properties'
                        env.URL     = props.url
                        env.USERID  = props.userid
                        env.PASS    = props.password
                        env.USERID2 = props.userid2
                        env.PASS2   = props.password2

                        echo """
                        🔹 Loaded Jenkins Managed Config
                        URL     : ${env.URL}
                        USERID  : ${env.USERID}
                        USERID2 : ${env.USERID2}
                        """
                    }
                }
            }
        }

        stage('Build') {
            steps {
                script {
                    echo "🔨 Running Maven Build..."
                    bat "${MAVEN_HOME} clean install -f ImportAndInwardBills\\pom.xml"
                }
            }
        }

        stage('Run Tests') {
            steps {
                script {
                    echo "🧪 Running TestCase: TC01_LodgeTheBillUsingImportandInwardMenu"
                    bat "${MAVEN_HOME} test -f ImportAndInwardBills\\pom.xml -Dtest=TC01_LodgeTheBillUsingImportandInwardMenu"
                }
            }
        }

        stage('Post Steps') {
            steps {
                script {
                    echo "✅ Pipeline completed successfully!"
                }
            }
        }
    }

    post {
        success {
            echo "🎉 Build and tests ran successfully!"
        }
        failure {
            echo "❌ Pipeline failed. Check logs for details."
        }
    }
}
